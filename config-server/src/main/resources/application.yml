spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/maicol1912/service-configuration-spring
          clone-on-start: true

server:
  port: 8888

